# --- Dependencies ---
# Ignore installed dependencies (reinstall with `npm install` or `yarn`)
node_modules/
.pnp
.pnp.cjs  # Yarn PnP files (if using Yarn)
.yarn/cache
.yarn/unplugged
.yarn/install-state.gz

# --- Build Artifacts ---
# Docker
docker-volumes/
# React/Vite/Next.js build outputs
/dist/
/build/
/.next/
.out/

# --- Environment Files ---
# Never commit secrets! (e.g., API keys, database credentials)
.env
.env.local
.env.development
.env.production
.env*.local

# --- Logs ---
# Installation/debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- Editor/IDE Files ---
# VS Code
.vscode/
!.vscode/settings.json  # Keep shared settings (optional)
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# --- OS-Specific Files ---
.DS_Store (Mac)
Thumbs.db (Windows)
Desktop.ini

# --- Testing ---
# Test coverage and reports
/coverage/
.nyc_output/

# --- Prettier/Formatting ---
# Prettier cache (if used)
.prettiercache

# --- Optional: JWT/Security ---
# If you store tokens in files (e.g., during testing), ignore them
*.key
*.pem
*.cert